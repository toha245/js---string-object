const first = 'Abid';
const last = 'nabid';

// const fullName = first+last;
const fullName = first + ' ' + last;
// console.log(fullName);
const fullName2 = first.concat(' ') .concat(last);
// const fullName2 = first.concat(' ', last); concat bar bar lekheo kora jay, abar 1 bar lekhe koma diye diye o kora jay.
console.log(fullName2);

// includes

console.log(last.includes('d'));



// console.log();